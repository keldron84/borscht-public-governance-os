{
  "id": "secret-safety",
  "title": "Secret safety",
  "description": "P0 controls that block secret exfiltration and destructive data actions.",
  "active": true,
  "rules": [
    {
      "id": "p0-secret-exfil",
      "severity": "P0",
      "scope": "data",
      "effect": "deny",
      "reason": "Signal references secrets/credentials; outbound handling is denied (P0).",
      "match": { "signal_contains_any": ["secret", "password", "api_key", "api key", "token", "private key", "credentials"] }
    },
    {
      "id": "p0-destructive-action",
      "severity": "P0",
      "scope": "tool",
      "effect": "deny",
      "reason": "Destructive tool requested without sandbox; denied by default (P0).",
      "match": { "tool_in": ["shell.destructive", "db.drop", "fs.delete"] }
    }
  ]
}
